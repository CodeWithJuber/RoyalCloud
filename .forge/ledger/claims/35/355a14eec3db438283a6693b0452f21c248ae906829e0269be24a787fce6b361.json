{"body":{"id":"D-0031","text":"Plan decks are one DOM with three CSS layouts — stacked ≤640, a two-up snap rail 641-1023 (the owner's choice over a squeezed grid), auto-fit ≥1024 with a 300px card floor — instead of a per-deck data-cols count; Rail measures its own overflow so controls and carousel semantics only exist where it scrolls"},"kind":"decision","scope":{"level":"repo"},"v":1}
