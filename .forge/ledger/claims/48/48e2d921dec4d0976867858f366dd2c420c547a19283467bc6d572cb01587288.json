{"body":{"id":"D-0015","text":"Feature cards accept optional metric/metricLabel (schema + Decap + renderer in one commit) rendered only when authored; the only seeded figure is shared-hosting's 15x, which the site's own stats band already states; the previously unused variant:tiles is now honoured for the four landing pages that author it"},"kind":"decision","scope":{"level":"repo"},"v":1}
