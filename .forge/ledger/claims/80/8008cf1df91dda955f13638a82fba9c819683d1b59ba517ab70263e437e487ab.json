{"body":{"id":"D-0025","text":"CMS login: the legacy Cloudflare worker answers 500 ('GitHub OAuth is not configured'), so the OAuth proxy is ported into the Next app (app/oauth/auth + /callback, lib/cms-oauth.ts) with GITHUB_OAUTH_CLIENT_ID/SECRET from the deployment env; config.yml now targets CodeWithJuber/RoyalCloud with base_url on the production origin"},"kind":"decision","scope":{"level":"repo"},"v":1}
