{"body":{"id":"D-0033","text":"repeat(auto-fit, minmax(min(100%, N), minmax(0, 1fr))) is invalid CSS — minmax() cannot nest — so the .grid-2/3/4 phone rule added in the Step 2 commit never applied; the duplicate is deleted rather than repaired, because the working one-column rule 3000 lines earlier is the right treatment for those prose cards"},"kind":"decision","scope":{"level":"repo"},"v":1}
