{"body":{"id":"D-0013","text":"One-click deploy tabs are driven by a static lib/deploy-catalog keyed by the names content already authors (no CMS schema change); descriptions/links exist only where copied verbatim from a deck highlight or the page's own copy (test-enforced), so the CyberPanel VPS page gets OS/panel/app tabs while OS-only pages keep the flat chip row"},"kind":"decision","scope":{"level":"repo"},"v":1}
