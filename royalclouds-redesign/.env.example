# Optional: set to a WHMCS/cart base if Royal Clouds changes its billing portal.
NEXT_PUBLIC_PORTAL_BASE=https://my.royalclouds.net

# Enable real network integration tests explicitly.
RUN_INTEGRATION=0
